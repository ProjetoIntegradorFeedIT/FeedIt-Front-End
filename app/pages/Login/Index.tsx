import { StyleSheet, Text, View, Image, ImageBackground } from 'react-native';
import theme from '../../themes/theme';
import { AreaTela } from './style';

export default function Login({navigation}: any) {
    return (
        <AreaTela>
            <Text>Entrou no Login</Text>
        </AreaTela>
    );
}